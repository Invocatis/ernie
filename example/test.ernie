


pi(pi_name):
  given:
  action: [database "principal_investigator" {name: pi_name}]

project(pi_name, project_name):
  given:
    pi: [pi pi_name]
  action: [database "project" {pi: pi, name: name}]
